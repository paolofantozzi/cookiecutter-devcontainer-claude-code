{% if database != 'none' %}
# Copy to `.env` and adjust. `DATABASE_URL` matches the credentials in `docker-compose.yml`
{% if db_is_server %}
# (bring the database up from inside the devcontainer with `docker compose up -d`).
{% else %}
# (a local SQLite file; no service to start).
{% endif %}
DATABASE_URL={{ database_url }}
{% endif %}
