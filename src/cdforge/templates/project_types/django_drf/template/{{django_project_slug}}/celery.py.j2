{% if include_celery %}
import os

from celery import Celery

os.environ.setdefault('DJANGO_SETTINGS_MODULE', '{{ django_project_slug }}.settings')

app = Celery('{{ django_project_slug }}')
app.config_from_object('django.conf:settings', namespace='CELERY')
app.autodiscover_tasks()
{% endif %}
