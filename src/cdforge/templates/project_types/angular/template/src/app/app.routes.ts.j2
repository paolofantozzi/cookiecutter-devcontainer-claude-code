import { Routes } from '@angular/router';

// Add the app's routes here. Prefer lazy standalone components, e.g.
//   { path: 'about', loadComponent: () => import('./about/about.component').then((m) => m.AboutComponent) }
export const routes: Routes = [];
